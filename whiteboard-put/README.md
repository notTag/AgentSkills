# whiteboard-put

Capture relevant context from the current session into the project's `whiteboard.md` before `/clear`, so the next session can ramp up fast.

## Trigger
`/whiteboard-put`, "save context", "checkpoint before clear", or signaling you're about to `/clear`.
