# whiteboard-get

Retrieve and display the contents of the current project's `whiteboard.md` scratch file.

## Trigger
`/whiteboard-get`, "show whiteboard", "read whiteboard", or "what's on the whiteboard".
