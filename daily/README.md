# daily

Summarize the current session into `~/.claude/daily-tracker.md`, then prompt to `/clear`. Records session start/end timestamps from the transcript (falls back to current time).

## Trigger
`/daily`, "log and clear", "summarize session".
