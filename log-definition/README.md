# log-definition

Log a definition Q&A from the current conversation as a standalone file in ClaudeBrain `knowledge/definitions/`.

## Trigger
`/log-definition`, "log that definition", or ask to save a definition.
