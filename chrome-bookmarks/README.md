# chrome-bookmarks

Convert a tabbed outline of links into a Chrome-importable bookmarks HTML file. Top-level lines are folder names; indented lines beneath them are URLs. Writes the result to `chrome_bookmarks.html` in the current working directory.

## Trigger
Provide a tabbed outline of folders + URLs and ask to make Chrome bookmarks.
